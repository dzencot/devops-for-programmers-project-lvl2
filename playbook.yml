---

- hosts: all
  remote_user: root
  roles:
    - role: dependencies
      become: yes

    - role: geerlingguy.pip

    - role: geerlingguy.docker
      when: is_install_docker | bool == true

    - role: deploy

    - role: datadog.datadog
      become: yes
